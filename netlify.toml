[build]
  publish = "public"
  command = "npm install && npm run purge && hugo --gc --minify --buildFuture"

[build.environment]
  HUGO_VERSION = "0.140.0"
  HUGO_EXTENDED = "true"

[context.deploy-preview]
  command = "npm install && npm run purge && hugo --gc --minify --buildFuture"