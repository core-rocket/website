[build]
  publish = "public"
  command = "hugo --gc --minify --buildFuture" 

[build.environment]
  HUGO_VERSION = "0.140.0"
  HUGO_EXTENDED = "true"
  
[context.deploy-preview]
  command = "hugo --gc --minify --buildFuture"